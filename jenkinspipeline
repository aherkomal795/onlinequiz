pipeline {
    agent any

    stages {
        stage('Clone Repository') {
            steps {
                git 'https://github.com/yourusername/onlinequiz.git'
            }
        }

        stage('Compile') {
            steps {
                sh 'javac OnlineQuizApplication.java'
            }
        }

        stage('Run Program') {
            steps {
                sh 'java onlinequizapplication.OnlineQuizApplication'
            }
        }
    }
}
